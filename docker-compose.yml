version: '3'

services:
  stock-predict-api:
    image: 'backend_autobot:latest'
    ports:
      - '8000:8000'
    volumes:
      - /var/www/AutoBotCk_Backend/.env:/app/.env
    deploy:
      mode: replicated
      placement:
        constraints: [node.role == manager]
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: any
    networks:
      - stock-predict_new

  stock-predict-postgres:
    image: postgres:15
    volumes:
      - /home/stock-predict-cms/postgres-15:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=x_stock
      - POSTGRES_USER=x_stock
      - POSTGRES_PASSWORD=stock123
    ports:
      - '5433:5432'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U x_stock -d x_stock"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      mode: replicated
      placement:
        constraints: [node.role == manager]
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: any
    networks:
      - stock-predict_new

networks:
  stock-predict_new:
    driver: overlay



# version: '3'

# services:
#   stock-predict-api:
#     build:
#       context: ./
#       dockerfile: Dockerfile
#     ports:
#       - '8000:8000'
#     volumes:    
#       - ./:/app/
#     environment:
#       - POSTGRES_DB=${DB_NAME}
#       - POSTGRES_USER=${DB_USER}
#       - POSTGRES_PASSWORD=${DB_PASSWORD}
#     deploy:
#       mode: replicated
#       placement:
#         constraints: [node.role == manager]
#       replicas: 1
#       update_config:
#         parallelism: 1
#         delay: 10s
#       restart_policy:
#         condition: any
#     networks:
#       - stock-predict_net

#   stock-predict-postgres:
#     image: postgres:15
#     volumes:
#       - /home/stock-predict-cms/postgres-15:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=${DB_NAME}
#       - POSTGRES_USER=${DB_USER}
#       - POSTGRES_PASSWORD=${DB_PASSWORD}
#     ports:
#       - '5433:5432'
#     deploy:
#       mode: replicated
#       placement:
#         constraints: [node.role == manager]
#       replicas: 1
#       update_config:
#         parallelism: 1
#         delay: 10s
#       restart_policy:
#         condition: any
#     networks:
#       - stock-predict_net

# networks:
#   stock-predict_net:
#     driver: bridge  # dùng bridge cho local
